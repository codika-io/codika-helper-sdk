# Test Run — 2026-03-31

## Environment

- CLI version: 3.2.0 (local build)
- Cloud Functions: deployed same day (listProjectsPublic, getProjectPublic, updateOrganizationApiKeyPublic, getProcessInstancePublic updated)
- Test org: `l0gM8nHm2o2lpupMpm5x`

## Results

| Category | Tests | Pass | Fail |
|---|---|---|---|
| `list projects` | 10 | 10 | 0 |
| `get project` | 12 | 12 | 0 |
| `get instance --workflows` | 12 | 12 | 0 |
| `organization update-key` | 13 | 13 | 0 |
| Scope enforcement | 4 | 4 | 0 |
| Cross-org isolation | 5 | 5 | 0 |
| Role-based access | 7 | 7 | 0 |
| **Total** | **63** | **63** | **0** |

## Issues Found During Testing

1. **Firestore index missing**: `listProjectsPublic` needed a composite index on `projects` (organizationId + archived + createdAt). Created via `gcloud firestore indexes composite create`. Took ~5 minutes to build.

2. **`createOrganizationApiKeyPublic` not redeployed**: After adding `projects:read` to `ALL_SCOPES`, the create-key function was still running old code and rejected `projects:read` as invalid. Fixed by deploying that function too.

3. **Limited key scope mutation**: The `update-key` functional test modifies the limited key's scopes. Tests must restore the original scopes after each mutation to avoid affecting subsequent tests.

## Access Control Verified

```
Owner sees:  14 projects, 15 instances
Member sees:  6 projects (own only), 7 instances (shared only)
Limited sees: 0 projects (scope blocked), 15 instances (has scope)
Cross-org:    blocked on all test org resources
```
